# -*- mode: python -*-

block_cipher = None


a = Analysis(['src/scheduler.py'],
             pathex=['/Applications/anaconda3/pkgs/pyqt-5.6.0-py36_2/bin', '/Users/Alex/Dropbox/projects/compsci-project'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='scheduler',
          debug=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='scheduler')
